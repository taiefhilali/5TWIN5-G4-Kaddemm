version: '3.3'

services:
  mysqldb:
    image: mysql:latest
    container_name: mysqldb
    volumes:
      - /home/mysql/data:/var/lib/mysql -d mysql
    restart: unless-stopped
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=kaddem
      - MYSQL_PASSWORD=root

  kaddem:
    image: zeinebharaketi/zeinebharaketi-5twin5-g4-kaddem
    ports:
      - "8089:8089"
    restart: on-failure
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysqldb:3306/kaddem?autoReconnect=true&failOverReadOnly=false&maxReconnects=10
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=root
    depends_on:
      - mysqldb
